version: '2'

services:
  ldap_server:
    image: erlangms/ldap_server
    ports:
    - 2389:2389
    shm_size: 420M
    stdin_open: true
    tty: true
    build: .
    volumes:
    - ./config/:/var/opt/erlangms/.erlangms/

