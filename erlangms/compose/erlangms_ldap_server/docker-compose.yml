version: '2'

services:
  ldap_server:
    image: erlangms/erlangms_ldap_server
    ports:
    - 2389:2389
    shm_size: 1G
    stdin_open: true
    tty: true
    build: .
    volumes:
    - ./odbc.ini:/var/opt/erlangms/.odbc.ini
    - ./config/:/var/opt/erlangms/.erlangms/

